# If you want Maven to populate this file from its properties, uncomment the following section, remove the other
# sections and remove the <filters> element from pom.xml.
# -----------------------------------------
jdbc.driverClassName=org.postgresql.Driver
jdbc.url=jdbc:postgresql://localhost:5432/dbunit
jdbc.username=dbunit
jdbc.password=dbunit

hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Needed by Hibernate3 Maven Plugin defined in pom.xml
hibernate.connection.username=dbunit
hibernate.connection.password=dbunit
hibernate.connection.url=jdbc:postgresql://localhost:5432/dbunit
hibernate.connection.driver_class=org.postgresql.Driver
